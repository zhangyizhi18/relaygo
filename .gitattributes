# 默认让 git 自行判定文本/二进制
* text=auto

# Windows 批处理必须是 GBK + CRLF：LF-only 会让 cmd.exe 的 goto/标签定位错乱
*.bat text eol=crlf
*.cmd text eol=crlf

# 二进制资源
*.png binary
*.jpg binary
*.jpeg binary
*.ico binary
*.gif binary
