# RelayGo 开源发布说明

> 本文档随开源包一同发布，请先读完「开源前必须完成的安全操作」一节再推送。

- **仓库**：`zhangyizhi18/relaygo`（Public）
- **许可证**：MIT
- **打包版本**：见 `relay_server/config.py` 的 `VERSION`
- **打包工具**：`tools/pack_opensource.py`（内部工具，不在本包内）
- **打包方式**：白名单收集 + 文本脱敏 + 两层出厂自检 + **全新 git 历史**

---

## 一、结论速览

| 项目 | 结果 |
|------|------|
| 收录文件 | 128 个（源码 + 文档 + 界面截图 + bat），约 771 KB |
| 排除文件 | 约 2.5 GB（其中 `release/` 产物 2.1 GB、原 git 历史 180 MB、第三方 SDK 约 90 MB） |
| 已脱敏 | 7 类真实凭据 / 主机 / 域名 / 路径，共 14 处替换 |
| 文档修正 | 补 MIT 徽章、许可证章节、授权验证说明、风险提示 |
| 结构自检 | 通过 |
| 内容自检 | 通过 |
| git 历史 | **全新**，仅 1 次初始提交（原仓库 67 次提交的历史全部丢弃） |
| 独立复核 | 用另一套规则集重扫 128 个文件，未发现任何泄漏特征 |

---

## 二、收录内容

```
relaygo/
├── README.md                    项目说明（已补许可证章节）
├── LICENSE                      MIT（新增）
├── NOTICE                       第三方组件 / 商标 / 免责声明（新增）
├── .gitignore / .gitattributes  新增
├── requirements.txt
├── docker-compose.yml           中转服务 Docker 部署（全为占位值）
├── 启动全部.bat / 启动中转服务.bat / 启动执行端.bat
├── 中转服务-配置.bat / 执行端-配置.bat
├── relay_server/                中转服务端（Flask + SQLite）
│   ├── app.py  store.py  task_api.py  reconcile.py  security_code.py
│   ├── config.py                版本号 + 各项默认值（凭据已换成占位值）
│   ├── env_template.py  envfile.py
│   ├── license_guard.py         卡密授权（对接自建授权服务端）
│   ├── web_console.py  web_ui.py  store_web.py   Web 控制台
│   └── Dockerfile  .dockerignore  requirements.docker.txt
├── executor/                    执行端（Windows，UI 自动化）
│   ├── main.py  tasks.py  config.py  risk.py  market_price.py
│   ├── broker_base.py  broker_ths.py  broker_thsauto.py  broker_miniqmt.py
│   ├── captcha.py  dialog_guard.py  foreground.py  pywinauto_compat.py
│   ├── failure_guard.py  ths_watchdog.py
│   └── env_template.py  envfile.py
├── joinquant/                   聚宽策略端
│   ├── jq_signal.py             ★ 信号上报包装（真实地址/密钥已换成占位值）
│   ├── jq_sim_gui.py  jq_sim_gui.json  聚宽信号模拟器（GUI）
│   ├── test_sim_fullflow.py  test_sim_gui_ui.py  test_sim_relay_e2e.py
│   └── 聚宽模拟器.bat
├── tools/                       开发与测试脚本（57 个 .py）
│   ├── test_signal.py  test_console.py  test_license_guard.py  ...
│   ├── jq_simulator.py          命令行版信号模拟器
│   └── test_ths_*.py  ths_*_probe.py   同花顺调试/探测脚本
├── release/                     发版脚本与模板
│   ├── build_all.py  build_exe.py  build_executor_exe.py  build_docker.py
│   ├── verify_release.py  release_common.py
│   ├── manual_template.html     用户手册模板
│   └── 升级说明.txt
└── doc/                         文档与界面截图
    ├── 用户手册-v1.3.0-*.html
    ├── 升级说明-v1.3.0-*.txt
    ├── F12拉起失败修复-2026-09-13.md
    ├── 执行端全流程测试与二轮修复报告-2026-09-13.md
    ├── 执行端测试报告-2026-09-13-三轮下单.md
    └── ui-preview/*.png         控制台界面截图（6 张）
```

---

## 三、排除内容与原因

### 1. 密钥与凭据

| 排除项 | 原因 |
|--------|------|
| `.env`、`.env.bak` | 真实运行配置：含真实券商资金账号、真实券商客户端安装路径、各密钥。程序首次启动会按 `env_template.py` 自动生成一份**带占位值**的 `.env`，无需入库 |
| `.git/`（原仓库 67 次提交、180 MB） | 原历史中曾提交过真实密钥与内网地址，**无法靠事后脱敏补救**。已丢弃全部历史，改为全新 `git init` |
| `MT5LicenseWeb2.4.py`、`a.env111` | 授权服务端源码与临时配置文件（工作区已删除，但仍留在旧历史里）。新仓库不含其历史，等于彻底移除 |
| `license_cache.json` | 授权缓存，绑定本机机器码 |

### 2. 主机与部署凭据

| 排除项 | 原因 |
|--------|------|
| `deploy/`（整个目录，35 个文件） | `relay_common.py` 里**硬编码了内网 NAS 的真实 SSH 主机/端口/用户名/明文密码**；`relay_browser_check.js`、`relay_stage4_verify.py` 含真实内网 IP；`deploy/staging/jq-relay/.env` 含真实的控制台会话密钥与管理员密码；另有 `*.tar.gz`、`*.png`、`.env.persist` 等部署产物。这些脚本只对作者自己的 NAS 有意义，不对外发布 |

### 3. 第三方 SDK

| 排除项 | 原因 |
|--------|------|
| `executor/xtquant/`（186 个文件，约 91 MB） | 迅投 QMT 官方 Python SDK `xtquant`（`xtquant_250807`，含 `*.pyd` / `*.dll`）。**版权属迅投，不从本仓库分发**。README 已说明使用者需自行从券商 QMT 客户端目录复制到 `executor/` 下 |

### 4. 体积与私有运行数据

| 排除项 | 原因 |
|--------|------|
| `release/_build/`、`release/v*/`、`release/*.log` | 打包产物与构建日志（约 2.1 GB）：可由 `release/build_all.py` 随时重建，且 build log 含本机绝对路径 |
| `executor/executor.log`（44 KB） | 实盘运行日志，含本机路径与交易痕迹 |
| `executor/captcha_samples/`（20 个文件） | 验证码识别样本 |
| `venv/`、`.workbuddy/`、`tmp.png` | 本地虚拟环境、IDE 工作目录、临时图片 |
| `joinquant/jq_signal.py.bak`、`joinquant/jq_sim_启动日志.txt` | 备份与运行日志，含真实中转地址残留 |
| `tools/pack_opensource.py`、`tools/_audit_oss.py` | 内部工具：打包器与独立复核脚本。它们源码里写着真实凭据字面量（用于替换与扫描），必须留在私有仓库 |

---

## 四、已做的脱敏（逐条）

对 128 个收录文件全部做了文本级替换（**字节级读写，不改换行符编码**，`.bat` 仍是 GBK + CRLF）：

| # | 类别 | 替换处数 | 涉及文件 |
|---|------|---------|---------|
| 1 | 聚宽信号密钥 → 示例占位值 | 1 | `joinquant/jq_signal.py` |
| 2 | 授权服务端 API Key → 占位值 | 1 | `relay_server/config.py` |
| 3 | 授权服务端域名 → `http://127.0.0.1:9527` | 3 | `relay_server/config.py`、`relay_server/env_template.py`、`release/manual_template.html` |
| 4 | 内网穿透隧道地址 → `http://127.0.0.1:5010` | 1 | `joinquant/jq_signal.py` |
| 5 | 真实 QMT 资金账号 → `1234567890` | 2 | `tools/test_miniqmt_offline.py` |
| 6 | 券商名称（QMT 目录名）→ 中性写法 | 2 | `executor/config.py`、`executor/env_template.py` |
| 7 | 本机兄弟项目绝对路径 → `<项目根目录>` | 4 | `tools/diag_foreground.py`、`tools/test_foreground_tray.py`、`tools/test_ths_autostart.py`、`tools/test_ths_broker.py` |

> 另有 2 类规则（内网 NAS 的 IP `192.168.x.x`、SSH 用户名与明文密码）准备了替换规则，
> 因 `deploy/` 整目录被排除，实际命中 0 处 —— 保留规则作为**防御性兜底**。

**同时确认无需改动**：`doc/ui-preview/*.png` 6 张控制台截图已逐张查看，全部是
`dry_run` 模拟账户的演示数据（可用金额 100000.00、备注「dry_run 模拟」、示例标的），
不含真实资金、持仓或委托。因此保留在包内。

**README 补丁**：补 MIT 徽章、`## 许可证` 章节、`## 关于「授权验证」`说明、`## 风险提示`。

---

## 五、⚠️ 开源前必须完成的安全操作

以下凭据**曾经存在于即将公开的代码库里**。虽然已从发布包中移除，但请按「已泄漏」处理，
在推送前完成轮换 —— 因为你的线上系统仍在用它们：

1. **轮换聚宽信号密钥**
   原值曾硬编码在 `joinquant/jq_signal.py`。请在中转服务的 `.env` 里改
   `RELAY_SIGNAL_KEY`，并同步改聚宽策略里的 `API_KEY`。
2. **更换 NAS 的 SSH 密码（最紧急）**
   `deploy/relay_common.py` 里是**明文**的内网主机 + 用户名 + 密码。请立即在 NAS 上
   改掉该账号密码，并考虑改用密钥登录、限制来源 IP。
3. **轮换授权服务端 API Key**
   原值曾硬编码在 `relay_server/config.py`。请在卡密服务端后台重置，并同步
   更新你私有仓库里的默认值。
4. **收紧那条内网穿透隧道**
   原隧道地址把中转服务暴露在公网。确认它已关闭，或至少限制来源与启用鉴权。
5. **推送前三行自检**（缺一不可）

   ```bash
   git ls-files | wc -l                       # 应为 128，与本说明一致
   git log --oneline | wc -l                  # 必须是 1
   git ls-files | grep -iE "\.env$|deploy/|xtquant/|\.bak$|\.log$"   # 必须无输出
   ```

6. **在 GitHub 仓库设置里开启**
   - `Settings → Code security → Secret scanning`（密钥扫描）
   - `Settings → Code security → Push protection`（阻止推送含密钥的提交）
   - `Settings → Code security → Dependabot alerts`

---

## 六、推送到 GitHub

仓库已在本包目录内 `git init` 并完成首次提交，只差一次 push：

```bash
cd relaygo
git remote add origin https://github.com/zhangyizhi18/relaygo.git
git push -u origin main
```

**建议的仓库设置**：

- Description：`聚宽云端策略 → 自建 Flask 中转 API → Windows 执行端 → 券商下单。支持同花顺 / miniQMT / 安卓模拟器三种下单方式，带 Web 控制台。`
- Topics：`joinquant` `quant-trading` `ths` `miniqmt` `xtquant` `easytrader` `flask` `automated-trading` `a-share`
- 确认 GitHub 已自动识别 License 为 **MIT**
- 建议给 `main` 分支加保护规则（禁止 force push）

---

## 七、许可证与合规要点

- 本项目采用 **MIT**，全文见 `LICENSE`。版权行为 `Copyright (c) 2026 RelayGo` ——
  如需改成你的真实姓名或 GitHub ID，请同步修改 `LICENSE` 与 `NOTICE` 两处。
- `NOTICE` 里列明了第三方组件许可证、**商标声明**（同花顺 / 迅投 / 聚宽均声明无隶属关系）
  与**免责声明**。这是这类「对接券商客户端 + 真实下单」项目必须保留的。
- `executor/xtquant/` 版权属迅投，**不得**重新提交到公开仓库。
- **关于 `license_guard`（卡密授权）**：源码公开意味着这道授权门槛任何人都能关掉。
  本包把服务端地址与 API Key 换成了占位值，但仍保留 `RELAY_LICENSE_ENABLED` 默认开启，
  所以**开箱即用会停在激活页**。你的正式发行版靠打包时写入的 `release.lock` 强制锁定该
  开关（见 `config.py` 的 `_license_release_locked()`），源码仓库没有这个文件 —— 官方发行
  版的授权链路不受影响。若你希望开源仓库开箱即用，在 `relay_server/config.py` 把
  `RELAY_LICENSE_ENABLED` 的默认值从 `1` 改成 `0` 即可（README 已写明这个开关）。
- **合规**：2025-07 起程序化交易需向券商完成首次报告后方可开展；README「上线路径」
  一节已保留该提示。

---

## 八、重新打包

```bash
python tools/pack_opensource.py        # 在私有仓库根目录执行
```

脚本会：清空输出 → 白名单收集 → 脱敏 → 生成 LICENSE/NOTICE/.gitignore/.gitattributes
→ 补 README → **两层自检（任一失败即中止，不产出压缩包）** → `git init` + 首次提交
→ 打 zip（排除 `.git/`）+ 打印 sha256。

输出：

```
../dist/relaygo/                     干净源码树（已提交，只差 push）
../dist/relaygo-src-<版本>.zip       发布压缩包
```

---

## 九、验收自检表

- [x] `git log --oneline` 只有 1 次初始提交，历史中无密钥
- [x] `git ls-files` 中无 `.env` / `deploy/` / `xtquant/` / `*.bak` / `*.log` / `*.pyd` / `*.exe`
- [x] 结构检查通过（禁止路径 / 后缀 / 缺失必需文件）
- [x] 内容检查通过（真实凭据 / 内网 IP / 域名 / 私钥头 / GitHub 令牌）
- [x] 独立规则集复核通过（127 个文件，0 命中）
- [x] `LICENSE` 已新增（MIT），`NOTICE` 已新增（第三方 / 商标 / 免责）
- [x] README 徽章与许可证章节与 `LICENSE` 一致（都是 MIT，无错写）
- [x] `.bat` 文件仍为 **GBK + CRLF**（LF-only 会让 `cmd.exe` 的 `goto` 错乱）
- [x] 本说明文档内**不含**任何真实凭据字面值，只写文件路径
- [ ] **轮换第五节列出的 4 项凭据** ← 推送前必须完成
- [ ] 在 GitHub 开启 Secret scanning + Push protection
